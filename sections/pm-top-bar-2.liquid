{%- comment -%}
  PM — Top Bar (second / lower row)
  ---------------------------------
  A second, independently-editable bar that stacks directly under the first one
  (both are rendered in layout/theme.liquid). Shares all markup + logic with the
  first bar via snippets/pm-top-bar-body.liquid. Off by default — turn on "Show
  top bar" to use it.
{%- endcomment -%}
{%- render 'pm-top-bar-body',
  settings: section.settings,
  blocks: section.blocks,
  section_id: section.id
-%}

{% schema %}
{
  "name": "PM — Top Bar 2",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Show top bar",
      "default": false
    },
    {
      "type": "header",
      "content": "Promotions"
    },
    {
      "type": "paragraph",
      "content": "Add Promotion blocks below to advertise sales in the bar. Active promotions replace the trust line; if you add more than one, they rotate. Tip: create the matching code in Discounts, then enter it here so shoppers see it."
    },
    {
      "type": "header",
      "content": "Trust line (shown when no promotion is active)"
    },
    {
      "type": "textarea",
      "id": "lead_text",
      "label": "Trust line (HTML supported)",
      "info": "Leave blank to use the default trust signals. Use <strong> for bold text.",
      "placeholder": "<strong>MBE-certified</strong> · Authorized HPE &amp; Intel partner · Free freight on orders over $5,000"
    },
    {
      "type": "number",
      "id": "free_ship_min",
      "label": "Free shipping minimum ($)",
      "default": 5000
    },
    {
      "type": "header",
      "content": "Design"
    },
    {
      "type": "paragraph",
      "content": "Leave any field blank to keep the default theme look. Colors apply to the whole bar; chip colors apply to the discount-code pill."
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color"
    },
    {
      "type": "color",
      "id": "chip_bg_color",
      "label": "Discount chip background"
    },
    {
      "type": "color",
      "id": "chip_text_color",
      "label": "Discount chip text"
    },
    {
      "type": "range",
      "id": "font_size",
      "label": "Text size",
      "min": 10,
      "max": 16,
      "step": 0.5,
      "unit": "px",
      "default": 11.5
    },
    {
      "type": "select",
      "id": "text_align",
      "label": "Promotion alignment",
      "options": [
        { "value": "flex-start", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "flex-end", "label": "Right" }
      ],
      "default": "flex-start"
    },
    {
      "type": "range",
      "id": "rotate_speed",
      "label": "Rotation speed (multiple promos)",
      "min": 2,
      "max": 10,
      "step": 0.5,
      "unit": "s",
      "default": 4.5
    }
  ],
  "blocks": [
    {
      "type": "promotion",
      "name": "Promotion",
      "settings": [
        {
          "type": "text",
          "id": "message",
          "label": "Message",
          "info": "e.g. Save 10% on all HPE servers"
        },
        {
          "type": "text",
          "id": "code",
          "label": "Discount code (optional)",
          "info": "Shown as a highlighted chip. Create the code in Discounts first so it works at checkout."
        },
        {
          "type": "text",
          "id": "start_date",
          "label": "Start date (optional)",
          "info": "Format YYYY-MM-DD. Leave blank to start immediately."
        },
        {
          "type": "text",
          "id": "end_date",
          "label": "End date (optional)",
          "info": "Format YYYY-MM-DD. Promotion auto-hides after this day. Leave blank for no end."
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    { "name": "PM — Top Bar 2" }
  ]
}
{% endschema %}
